# react-scripts always builds with NODE_ENV=production, which loads this
# file. Default to staging so a plain `npm run build` can never accidentally
# ship pointing at the real dona-patys-kds project. Real prod builds must
# explicitly run `npm run build:production` (see package.json), which
# overrides this via a shell-set env var.
REACT_APP_FIREBASE_ENV=staging
